<!--<h2>Sign in21</h2>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, :required => false, :autofocus => true %>
    <%= f.input :password, :required => false %>
    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>-->



<div class="container" style="margin-top: 50px; border: 0px solid red; ">
		<div class="span4 well" style="margin-left: 320px; border: 0px solid green; ">
			<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "", style: 'border: 0px solid red; '}) do |f| %>
  			<legend style="border: 0; text-transform: capitalize; color: #666; ">
          <i class="icon-coffee" style="padding-right: 5px; font-size: 18px; "></i>
          Login
        </legend>
        
        <% flash.each do |key, value| %>
      		<div class="alert alert-<%= key %>">
        		<%= value %>
      		</div>	
    		<% end %>

        <%= f.input_field :email,
                          placeholder: 'email',
                          class: 'span4' %>
  			

        <%= f.input_field :password,
                          placeholder: 'senha',
                          class: 'span4' %>

        <%#= f.input :remember_me,
                    as: :boolean,
                    wrapper: 'basic',
                    label_html: { style: 'color: #666; font-size: 13px; ' } %>

        <%= button_tag(type: 'submit',
                       class: 'btn btn-block btn-primary',
                       style: 'margin-top: 10px; font-weight: bold; ') do %>
          login
          <i id="icon-loading"></i>
        <% end %>
      <% end %>

			</form>
      
      <!--<div class="devise_links">   
        <%= render "devise/shared/links" %>
      </div>
		</div>-->


</div>
