


<% if params[:budgets_product][:quantity] %>

	<% price = compute_price(@product.supplier_price, @product.ipi, @budgets_product.quantity, @budgets_product.quantity) %>

	$('#product_id_<%= @budgets_product.product_id %>').find('.td_quantity').html('<%= @budgets_product.quantity %>');
	$('#product_id_<%= @budgets_product.product_id %>').find('.td_price').find('span').html('<%= number_to_currency(price) %>');
	$('#product_id_<%= @budgets_product.product_id %>').find('.td_price').find('i').addClass('icon-ok success_text');

	hide_all_forms();
	show_spans();

<% end %>






	$('#div_table_results').html('<%= escape_javascript(render partial: "table_results") %>');




make_td_same_width_for_budget_tables();